# EngiFormat

EngiFormat 是一个面向嵌入式 UI 开发者的本地图片格式转换工具。

它专注于解决绘图软件无法精确导出单片机所需图片格式的问题，提供图片预览、缩放、位深控制和稳定导出能力，并支持 Windows、macOS、Linux。

## 开发

```bash
npm install
npm run tauri dev
```

## 验证

```bash
npm test
npm run build
```
